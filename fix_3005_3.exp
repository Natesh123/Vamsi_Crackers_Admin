#!/usr/bin/expect -f
set timeout -1
set password "Prasath@1990"
set host "103.191.208.98"
set user "root"

spawn ssh -o StrictHostKeyChecking=no $user@$host "ss -lptn > /tmp/ports.txt; cat /tmp/ports.txt"
expect {
    "password:" { send "$password\r"; exp_continue }
    eof
}
